import React from "react";

class Button extends React.Component{
    render() {
        return(
            <button>
                botao
            </button>
        )
    }
}

export default Button;